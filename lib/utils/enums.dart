enum Gender { trans, female, male }

enum PickType { image, media, video }

enum ButtonType { raised, text, border }

enum AlertGroupType { all, group, specific }

enum FieldType { text, radio, dropDown, checkBox }

enum APIMode { dev, integration, test, staging, prod }

enum TransitionType { normal, fade, scale, slide, size, rotate }

enum HTTPMethodType { delete, download, fetch, head, patch, post, put }

enum LoaderType {
  chasingDots,
  circle,
  cubeGrid,
  dancingSquare,
  doubleBounce,
  dualRing,
  fadingCircle,
  fadingCube,
  fadingFour,
  fadingGrid,
  foldingCube,
  hourGlass,
  normal,
  pianoWave,
  pouringHourGlass,
  pouringHourGlassRefined,
  pulse,
  pulsingGrid,
  pumpingHeart,
  ring,
  ripple,
  rotatingCircle,
  rotatingPlain,
  spinningCircle,
  spinningLines,
  squareCircle,
  threeBounce,
  threeInOut,
  wanderingCubes,
  wave,
  waveSpinner
}
